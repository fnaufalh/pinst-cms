'use strict';

/**
 * maintenance-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maintenance-project.maintenance-project');
