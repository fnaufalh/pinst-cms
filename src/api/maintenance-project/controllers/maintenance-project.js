'use strict';

/**
 * maintenance-project controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::maintenance-project.maintenance-project');
